---
name: platform-mark
description: Deprecated alias — use /docs-mark (ArtifactGraph).
disable-model-invocation: true
---

# /platform-mark → /docs-mark

**Deprecated.** Member tag/registry annotation moved to **`/docs-mark`**
(ArtifactGraph common harness).

Run `/docs-mark` instead. This stub remains for one compatibility cycle, then
will be removed from the package harness.
