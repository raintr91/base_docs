# Module legacy dynamics IR — portal-legacy-dynamics/v1
# Copy to product/legacy-dynamics/{module}/_legacy.dynamics.yaml then fill.

schema: portal-legacy-dynamics/v1
module: role-domain

legacy:
  repo: example-legacy-repo # projects.*.repo slug in platform-repos / legacy-repos
  commit: null

tracedAt: 1970-01-01T00:00:00Z # set on /legacy-spec write
tracedBy: legacy-spec

index: {}
# hotel-list:
#   slice: slices.hotel-list
#   legacyRoute: GET /admin/hotel/list
#   controller: Admin\\HotelController@index

slices: {}
refs: {}
